import 'package:flutter/material.dart';

class SearchAndSortPage extends StatelessWidget {
  const SearchAndSortPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        body: Container(),
      ),
    );
  }
}
